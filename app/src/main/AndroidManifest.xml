<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.modori.kwonkiseokee.AUto"


    android:versionName= "1.5.2">

    <!--버전 코드 설명 https://mainia.tistory.com/4925-->

    <!-- 배경화면 설정 권한 추가 -->
    <uses-permission android:name="android.permission.SET_WALLPAPER" /> <!-- 기기에 저장되어 있는 것들 읽기 권한 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/appicon_default"
        android:label="@string/app_name"
        android:roundIcon="@drawable/appicon_rounded"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".LicensePage" />
        <activity
            android:name=".SplashActivity"
            />
        <activity android:name=".SelectFolder" />

        <!--<activity android:name=".autoWallpaper" />-->
        <activity android:name=".oldSetWallpaper.pickon_recyclerview" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".SetWallpaperJob" />

        <receiver
            android:name=".Receiver.BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:label="BOOTReceiver">

            <intent-filter >
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>